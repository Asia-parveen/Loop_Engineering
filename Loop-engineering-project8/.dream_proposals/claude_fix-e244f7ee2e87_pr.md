## Automated Improvement Proposal (Dream Loop)

**Change Type:** Modify
**Target File:** `src\connector.py`

### Evidence
STATUS: CONNECTOR_SKIPPED
Failure 'CONNECTOR_SKIPPED: Connector did not ship (idempotent or failed)' occurred 5 times on dates: 2026-08-29T00:55:33, 2026-08-29T00:55:51, 2026-08-29T00:56:48, 2026-08-29T00:58:11, 2026-08-29T00:58:43. Example: Failed to create branch

### Proposed Change
```diff
--- a/src/connector.py
+++ b/src/connector.py
@@ -74,7 +74,10 @@ def create_branch(repo_root: Path, branch_name: str, base_branch: Optional[str] = None) -> bool:
     """Create a new branch from base_branch."""
     if base_branch is None:
         base_branch = get_default_branch(repo_root)
-    # Fetch latest
+    # Fetch latest (with fallback if origin/HEAD not set)
     run_git(repo_root, "fetch", "origin", base_branch)
-    # Create branch
+    # Create branch - try with origin/base, fallback to local base
+    result = run_git(repo_root, "checkout", "-b", branch_name, f"origin/{base_branch}")
+    if result.returncode != 0:
+        result = run_git(repo_root, "checkout", "-b", branch_name, base_branch)
     return result.returncode == 0
```

### Validation
- [ ] Maker-checker validation passed
- [ ] Evidence cited with exact run dates and frequency
- [ ] Smallest possible change to address the issue
- [ ] No direct modification of rules files (PR only)

---
*Generated by Loop Engineering Project 12 — Dream Loop*
